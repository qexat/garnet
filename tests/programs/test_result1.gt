-- Format:
--   status: success
-- Compile:
--   status: error

type Result(T, E) = sum
    Ok T,
    Err E,
end

-- TODO: This should probably succeed
-- related: test_sum3.gt

fn main() {} =
    let x = Result.Ok 42
    let y = Result.Err true
    let res = x == y 
    __println_bool(res)
    {}
end
