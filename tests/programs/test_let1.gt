-- Compile:
--   status: success


type Foo(@Thing) = struct
    a: @Thing,
    b: Bool
end

fn id(x @T) @T =
    x
end


fn main() {} =
    let f1 = id(Foo {
        .a = 3_I32,
        .b = false
    })

    /- TODO: This passes type checking but then fails
    codegen, make a test to pin it down.
    let f1 = id(Foo {
        .a = 3,
        .b = false
    })
    -/

    let f2 = id(Foo {
        .a = true,
        .b = false
    })

    let f3 = f1
end
