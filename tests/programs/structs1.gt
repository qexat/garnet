-- Compile:
--   status: success
--
-- Run:
--   status: success

struct Bar {
}

struct Foo {
    x: I32,
    y: I16,
    z: I8,
}

fn main(): {} =
    let f: Foo = Foo {
        x = 1,
        y = 2,
        z = 4,
    }
    --let g: Bar = Bar {}
    --__println(f.z)

end
