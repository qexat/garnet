-- Compile:
--   status: success

fn identity(i @T) @T =
    let y @T = i
    y
end

fn foo(i @T[3]) @T[3] =
    i
end

fn main() {} =
    let a I32[4] = identity([1, 2, 3, 4])
    -- TODO: Typechecker allows this to pass as UnknownInt[3]
    let b I32[3] = foo([1, 2, 3])
end
